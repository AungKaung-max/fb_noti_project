{
  "name": "production",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "facebook",
    "server"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm --prefix facebook run build && npm --prefix server run build",
    "start": "npm --prefix server start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
